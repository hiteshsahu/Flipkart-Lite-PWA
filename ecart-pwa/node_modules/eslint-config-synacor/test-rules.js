'use strict';

// Rules to use/override for unit/e2e tests
var tests = {
	"compat/compat": 2 //don't worry about browser compatibilty in our unit tests
};

var testRules = {
	rules: Object.assign({}, tests)
};

module.exports = testRules;
